import { Box, Container, Typography } from "@material-ui/core";

const Greeting = () => {
    return (
        <Box>
            <Container>
                <Typography variant="h1">
                    Greeting
                </Typography>
            </Container>
        </Box>
    );
};

export default Greeting;
